[package]
name = "automata_rust"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
rand = "0.10.0"
getrandom = { version = "0.4", features = ["wasm_js"] } 
